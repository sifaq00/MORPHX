# Narrative Placeholder Only

Folder ini sengaja ditambahkan sebagai gimmick/concept-only placeholder.

Folder ini bukan runtime aktif untuk repo ini.

Aturan:

- jangan import kode dari sini ke `web/`
- jangan arahkan script root ke folder ini
- jangan gunakan folder ini sebagai backend lokal

Runtime aktif tetap ada di:

- `web/index.html`
- `web/vite.config.js`
- `web/server/generate-token.js`
- `web/server/vite-api-plugin.js`
- `web/api/generate.js`

Folder ini hanya menjaga product story `@morphx/server` tetap terlihat di struktur repo tanpa memengaruhi aplikasi yang berjalan.
