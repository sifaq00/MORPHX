{
  "name": "morphx",
  "version": "1.0.0",
  "private": true,
  "description": "MORPHX - AI Token Concept Generator & MCP Server for pump.fun",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "start": "npm --prefix web run preview"
  }
}
